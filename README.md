# Chess
A fully functional chess game built from scratch in Python using the turtle library. It implements complete chess rules including legal move validation, check detection, castling, and en passant. The project focuses on learning game logic, state management, and algorithms through manual implementation without external chess libraries.
